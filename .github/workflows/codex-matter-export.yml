name: Stage audited Matter payload

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staged:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Audit payload is being staged through the connected repository API."
