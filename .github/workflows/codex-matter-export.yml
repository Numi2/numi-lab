name: Stage complete lazy rod pipeline patch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staged:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The complete lazy rod-family patch is being staged."
