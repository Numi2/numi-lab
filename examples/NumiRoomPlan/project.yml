name: NumiRoomPlan
options:
  bundleIdPrefix: lab.numi
  deploymentTarget:
    iOS: "17.0"
targets:
  NumiRoomPlan:
    type: application
    platform: iOS
    sources:
      - path: NumiRoomPlan
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: lab.numi.roomplan
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        PRIVACY_POLICY_URL: ""
        INFOPLIST_FILE: NumiRoomPlan/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        SWIFT_VERSION: 5.0
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTS_MACCATALYST: NO
    scheme:
      testTargets: []
